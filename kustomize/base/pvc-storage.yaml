apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-uniselec-storage
  labels:
    app: uniselec-api
    component: storage
  annotations:
    nfs.io/storage-path: ""
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi