apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mariadb
spec:
  template:
    spec:
      containers:
        - name: mariadb
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "3000m"
              memory: "5Gi"